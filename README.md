# aws-iot-pi-edge-device
This code implements an AWS IoT edge device control program for Raspberry Pi.  It listens for commands published to a topic, 
performs the actions, and (optionally) returns a respsonse.
